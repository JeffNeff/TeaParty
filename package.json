{
  "name": "public",
  "version": "1.0.0",
  "description": "```\r docker compose up -d\r docker compose -f docker-compose.user.yaml up -d \r ```",
  "main": "buy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3"
  }
}
